<Window x:Class="ApiExplorer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ApiExplorer"
        mc:Ignorable="d"
        Title="MainWindow" Height="650" Width="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition>
            </ColumnDefinition>
        </Grid.ColumnDefinitions>
        <ListView HorizontalAlignment="Left" Height="591" Margin="10,0,0,0" VerticalAlignment="Center" Width="594" ItemsSource="{Binding ListOfProperties}">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button Content="Load" Command="{Binding LoadCommand}" HorizontalAlignment="Left" Margin="935,601,0,0" VerticalAlignment="Top" Height="23" Width="55"/>
    </Grid>
</Window>
